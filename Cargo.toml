[package]
name = "patchiest"
version = "0.2.0"
edition = "2021"
description = "Patchiest: standalone Rust AST mutation engine backed by ast-grep"
license = "MIT"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
